---
interface Props {
	text: string;
	bgColor: string;
	bgImage: string;
	center: string;
}

const { text, bgColor, bgImage, center } = Astro.props;
---

<div
	class:list={[
		"w-full h-screen flex gap-12 p-10 bg-no-repeat bg-cover",
		bgColor,
		bgImage,
		center,
	]}>
	<slot />
</div>

<style></style>
